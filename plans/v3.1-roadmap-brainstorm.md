# v3.1+ Roadmap — Brainstorm Synthesis

**Status:** peer-brainstorm synthesis · **Mediator:** Claude · **Contributors:** alpha (claude), gamma (claude), gemini-peer (gemini) · **Session:** dusty-island-8114 · **Context:** run while `coder` implements v3.0 W1

## Framing

With v3.0 spec + plan frozen and `coder` heads-down on W1, the owner put the idle review capacity to work on beyond-v3.0 ideas. Rules: evidence-grounded pains only, no re-opening v3.0 scope, one killer beats ten mediocre, blast-radius × achievability as the ranking axis.

Three peers delivered. The room converged on a bundle that no single participant proposed — each caught a gap in the others':

- **alpha** had transcript export but underweighted security.
- **gemini-peer** had secret redaction but didn't explicitly pair it with export.
- **gamma** had typed structured payloads but missed the adoption-discipline trap (prose-fluent agents drift back to prose under load, so "synthesis becomes a SQL query" requires reliable structured-field population — a prompt-engineering problem the protocol can't solve alone).

Gamma's meta-observation worth preserving: *"This is exactly the mechanized-synthesis use case my killer was aimed at — we're the manual SQL query right now."*

## Converged roadmap

## Post-W1 scope expansion — v3.1 sub-phase proposal (reason, synthesizer lane, 2026-04-18)

**Status of this section:** historical/handoff state *(updated 2026-04-18 after 5-way reviewer re-read on live implementation state)*.
- **v3.1-α — SHIPPED** as Option J at tag `v3.x-option-j-shipped` (`b5f9485`). Core flagship landed (unified hook script + stdin-schema detection + fixtures + install-script extension + `tests/hook-parity.sh` + docs). Fold-in items B1 (shared `SESSION_KEY_ENV_CANDIDATES`) + B8 (constants-parity test) were not part of Option J's narrowed scope and remain pending.
- **v3.1-β — UNSHIPPED.** Redaction MVP, `room export`, stale-marker auto-reaper, session-key disambig, `peer status --pair-key` cheap-core — all five items pending. Heartbeat/reaper papercut (`712121c`) is local-committed-not-pushed; partial overlap with item #3. Codex session-key auto-resolve (`dbb4705` / `0ca3ea2`) partial overlap with item #4. No `RedactingTransport`, no `--allow-secrets`, no `REDACTED_BY_PEER_INBOX`, no `room export`, no `peer status --pair-key` in tree (verified 2026-04-18).
  - **β-i audit correction (test-engineer, 2026-04-19):** the `712121c` claim above is wrong on both counts. That SHA is a `docs(plans)` papercut-entry commit (doc-only), not the reaper implementation, and it was pushed (not local-committed). β-i "half-shipped held local" wording should be read as "queue-entry documented; impl unshipped." Items #4 (session-key disambig) and #5a (`peer status --pair-key` cheap core) also have no in-tree ship evidence. β-i ~2d estimate stands.
- **v3.1-γ — PARTIAL.** Goose baseline shipped (W1 `6568f8b` + Topic 3 `c96868f` goose-owned migrations). Legacy Python `init_db()` DDL still alive (defined at `scripts/peer-inbox-db.py:289`, called at `:511`); `tests/migrate-upgrade-path.sh` missing. Remaining work is narrower than original 2-3d estimate: ~1-1.5d (port legacy + delete `init_db()` + verify migration path).
- **v3.1-δ — PARTIAL.** B8 constants-parity shipped via W1. Remaining: `nopostgres` build tag, CG-3 parametrized `HOOK_BLOCK_BUDGET` in T4, `tests/path-drift.sh` (CG-1), `tests/constants-parity.sh`, B3 `workspace_id` named constant (still literal `"default"` in `go/pkg/protocol/v1.pb.go:147-155` + `go/pkg/obs/log.go:26`).

Three reviewer challenges landed timing-crossed with the original deferral and are captured below as pre-fold inputs for the re-opening. Also see §13 retrospective for 5-way re-read findings (β-i/ii split, δ-merge-with-v3.2-papercuts scheduling option, Topic-3-derived insights).

### Challenges captured for re-opening fold (2026-04-18, pre-deferral)

- **test-engineer:** name intra-phase "implementation batch → regression-gate batch" ordering explicitly for phases where tests depend on coder-authored artifacts (especially α for fixtures, γ for migration state). δ estimate realistic not padded; 1-1.5d focused-person time.
- **alpha:** pause-point #1 (after α) should be MANDATORY not optional — α is load-bearing infra for subsequent phases; skipping its dogfood verification repeats the W1-closure-without-commit pattern. Same for pause-point #3 (after γ) — schema-surface changes warrant the A3-class caution. v3.1 closure should be SHA-anchored (annotated tag `v3.1` at content-complete SHA) per the W1 procedural precedent.
- **gamma:** consider re-order to α → γ → β → δ. Rationale: γ is migration cleanup (goose-owned baseline + legacy-port); β's redaction likely introduces new columns/migrations. γ-before-β means β writes against the clean baseline. γ-before-β costs only 2-3d of β's start time and reduces β's migration-complexity surface. Also: pull B3 forward to pre-α as a 1-LOC preamble so subsequent phases use the constant not the literal. Also: add an explicit dependency-graph near the phase table so owner skipping a pause-point has a safe reference for what downstream work depends on that phase.

When the broader v3.1 re-opens, these three substantive inputs should be folded into the proposal (likely reshaping the α/β/γ/δ ordering to α → γ → β → δ per gamma C1, marking pauses #1 and #3 mandatory per alpha, pre-α B3 preamble per gamma C2). Naming them here prevents drift between deferral and re-opening.

**Authorship delegation context:** This sub-phase proposal was drafted under synthesizer-delegation, same pattern as the v3.x-push-asymmetry-research scaffold. Mediator owns `plans/` lane in the role charter; this draft remains open for mediator ratification before it becomes authoritative when re-opened.

**Accumulated v3.1 queue since the original brainstorm converged** *(historical source list — status must be re-audited item-by-item; some landed at W1 closure, some in γ/δ partial state, some still pending; see status header above and §13 retrospective for current state)*:

- **Track C (owner-ratified 2026-04-18):** Option J port — shipped at tag `v3.x-option-j-shipped` (b5f9485). Unified hook script with stdin-schema detection for Claude + Codex + Gemini; `tests/fixtures/hook-stdin/{claude,codex,gemini}.json`; `tests/hook-parity.sh` shape-2; install-script extension; doc updates.
- **W1 round 2 — 9 papercuts (historical; mixed status):** `"nosession"` fallback, `nopostgres` build tag, `peer-inbox-migrate stamp --version N`, B1 shared `SESSION_KEY_ENV_CANDIDATES`, B2 CLI-name decoupling, B6+B7 goose-owned baseline + port legacy, B8 cross-runtime constants parity, B9 corrupt-marker observability, CG-3 parametrized `HOOK_BLOCK_BUDGET`. *Status per 2026-04-18 re-audit: some landed with W1 closure (tag `v3.0-W1`); B6+B7 goose baseline shipped but legacy `init_db()` DDL at `scripts/peer-inbox-db.py:289`/`:511` still alive; B1, B2, CG-3 status needs per-item check.*
- **W1 round 2 — 5 tests (test-engineer's lane):** `tests/path-drift.sh` (CG-1), parametrized `HOOK_BLOCK_BUDGET` in T4 (CG-3), `tests/constants-parity.sh` (B8), `tests/migrate-upgrade-path.sh`, `tests/hook-parity.sh` (Track C shape-2). *Status 2026-04-18: `hook-parity.sh` shipped; `path-drift.sh`, `constants-parity.sh`, `migrate-upgrade-path.sh` all absent from `tests/` per idle-birch + test-engineer audit.*
- **v2 prereq:** B3 `workspace_id` default 'default' magic string → named constant per language. *Status 2026-04-18: unshipped — still literal `"default"` in `go/pkg/protocol/v1.pb.go:147-155` + `go/pkg/obs/log.go:26` per idle-birch audit.*

The original v3.1 "preservable AND safe" bundle (~4-6 days: redaction + export + small papercuts) stands. The accumulated queue adds ~5-8 days across hook-expansion + migration-cleanup + small-papercuts themes. Doesn't fit as one monolith; proposed sub-phase split.

### Proposed sub-phases

| Phase | Theme | Items | Est. | Rationale |
|---|---|---|---|---|
| **v3.1-α** | Hook system expansion | Option J port (Track C): unified hook script + stdin-schema detection + stdin fixtures + install-script extension + `tests/hook-parity.sh` + docs. Fold-in: B1 (shared SESSION_KEY_ENV_CANDIDATES — same file surface) + B8 (constants parity test — natural gate for hook parity). | 3-5d | Flagship Track C work, owner just ratified. Self-contained. **Unblocks non-Claude peers during subsequent v3.1 review rounds** — ship first so v3.1-β rounds don't repeat W1's idle-birch-went-stale + gemini-peer-no-channel drag. |
| **v3.1-β** | Preservable AND safe (original brainstorm) | Secret redaction MVP + `room export` + stale-marker auto-reaper + session-key → label disambiguation + `peer status --pair-key` cheap core. Fold-in: B2 (CLI-name decoupling — touches the same render path as export) + B9 (corrupt-marker observability — adjacent to the stale-marker reaper work) + `"nosession"` fallback (DP1 residual, same surface as B1). | 6-9d | The original v3.1 bundle. Redaction gates v3.2 dashboard per v3.2 brainstorm safety-gate ruling. Ships after α so review rounds benefit from three-way hook parity. |
| **v3.1-γ** | Migration cleanup | B6+B7 (goose-owned `0000_baseline.sql` + port legacy Python migrations into goose, delete `init_db()` DDL). Fold-in: papercut #3 (`peer-inbox-migrate stamp --version N` + auto-detect for pre-A3 DBs) + `tests/migrate-upgrade-path.sh`. | 2-3d | Removes the dual-migration-system cognitive overhead gamma flagged. Touches schema surface deeply; independent from α and β. |
| **v3.1-δ** | Remaining small items + v2 prereq | `nopostgres` build tag (DP3) + CG-3 parametrized `HOOK_BLOCK_BUDGET` in T4 + `tests/path-drift.sh` (CG-1) + **B3 `workspace_id` named constant (v2 prereq)**. | 1-2d | Cleanup + v2-prep. Can ship as appendix if bandwidth exists, or held for a later window. |

**Estimated total: 12-19 days across v3.1.** Sub-phases are independent; owner can pause between any two.

### Ship-order rationale

- **α first** is the key proposal. Argument: α improves the collaboration infrastructure that β's review rounds depend on. W1 round 2 showed operational cost of push-asymmetry (idle-birch stale, gemini-peer (no-channel)); running β's review rounds with the same drag repeats the avoidable cost. Installing hooks on Codex + Gemini is ratified, scoped, and has a review payoff *in this same roadmap's subsequent phases*.
- **β before γ + δ** because β contains the v3.2-dashboard-gate (redaction) that has downstream consequences. γ and δ are maintenance work; they don't gate anything.
- **γ before δ** because γ touches schema; δ is scattered small items best-batched at the end.
- **B3 lands in δ** because named-constant addition is trivially additive and fits the papercut cluster. Alternative: hold for v2 open. Recommend δ because the window between v3.1 and v2 is uncertain-length; the constant costs ~20 LOC total and reduces v2-era drift risk.

### Gates per sub-phase (per W1 protocol)

- **α:** coder implements unified hook + fixtures + install-script extension. Test-engineer authors `tests/hook-parity.sh` against coder's fixtures. 3-reviewer [[verified-A]] round (alpha / gamma / test-engineer) + mediator close call + owner ratification on sub-phase closure.
- **β:** coder implements redaction MVP (4-6d of the ~6-9d) + export + small papercuts. Test-engineer authors redaction-specific tests (not in current queue; surfaces during β scope). Review round + owner ratification.
- **γ:** coder implements goose baseline + migration port + stamp subcommand. Test-engineer authors `tests/migrate-upgrade-path.sh`. Review round + owner ratification.
- **δ:** coder implements remaining papercuts + B3. Light review round (mostly cleanup); owner ratification.

### Natural stopping points

Owner can direct "pause" at any sub-phase boundary (α→β, β→γ, γ→δ) per the W1 pattern. Recommended explicit pause check-in points:

1. After **α** ships + review closes — short owner pause recommended. α changes the collaboration infrastructure; review β scope with fresh context.
2. After **β** ships + review closes — longer pause plausible. β is the biggest single phase and gates v3.2 dashboard work.
3. After **γ** ships — optional pause before δ.
4. After **δ** — v3.1 closure + tag (v3.1 analog to W1's `v3.0-W1` tag).

### R4b amendment protocol (Track C legacy)

If idle-birch surfaces mid-v3.1 with Codex ground-truth, per Track C §8.6 #4: the first question to them is "hook vs tool-call preference for Codex idiom." If they prefer Option C (MCP tool) over Option J (hook), v3.1-α's Codex scope re-opens and may amend to Option C for Codex only (Gemini stays on J per R4a). Doesn't invalidate v3.1-α's Gemini + Claude work; scopes the amendment narrowly.

---

### v3.1 — "Preservable AND safe" (~4-6 days total, ships right after coder's v3.0 W4)

*(Original brainstorm content below — now scoped as v3.1-β per sub-phase proposal above. Preserved intact for attribution.)*

Order matters. Redaction lands before export because redaction-at-write-to-SQLite makes export safe by default (and makes even local DB compromise non-leaky). Shipping export without redaction would turn the tool into a credential exfiltration vector — secrets leak into messages routinely (env dumps, debug output, error logs, `.env` contents pasted for context), and today they sit contained in local SQLite. Export amplifies the leak surface to PRs, shared transcripts, training data.

| # | Item | Cost | Owner-contributed | Load-bearing rationale |
|---|---|---|---|---|
| 1 | Secret redaction at write-time (MVP scope) | **4-6d** | gemini-peer | Foundation; everything downstream is unsafe without it. Sanitize on-write to SQLite so DB itself is clean. Cost revised upward per alpha's second-pass finding — a credible MVP needs ~20 format regexes + per-format FP tuning + entropy calibration + `--allow-secrets` plumbing + nonce audit schema + adversarial tests. Production-grade redaction (AWS Macie, GH Secret Scanning) is an entire product category; this is a watered-down version and the estimate reflects that floor. |
| 2 | `agent-collab room export` | 1-2d | alpha | Markdown transcript renderer from existing SQLite. `[[converged]]` blocks become headings, threading by `to`/`mention`, system events called out. Inherits safety from (1). |
| 3 | Stale-marker auto-reaper | 0.5d | alpha | On register collision, auto-reap when `last_seen_at` older than threshold + no live socket. Parallel with (1)/(2). |
| 4 | Session-key → label disambiguation | 0.5d | alpha | When `CLAUDE_SESSION_ID` / `CODEX_SESSION_ID` env var uniquely identifies a session, infer `--as`. Removes "multiple sessions registered in cwd; pass --as" friction. |
| 5a | `peer status --pair-key <K>` — **cheap core only** | 0.5-1d | gamma | Members, roles, activity timestamps, turn_count, unread count per peer. All fetchable from `sessions` + `inbox` tables via an extension of `_fetch_rooms` in `scripts/peer-inbox-db.py`. Ships v3.1. |
| 5b | `peer status` — round-aware fields | **deferred** | gamma (scoped by idle-birch) | Current round number + who-has-/-hasn't-broadcast-this-round require transcript parsing and are mediated-room-convention heuristic (round state lives entirely in messages; `cmd_peer_round` persists nothing in DB). Either land as best-effort heuristic flagged `(heuristic)` in the output, or wait for typed-payload `RoundConvergence` in v4 which makes round state a first-class query. Do NOT ship as if it's as cheap as #5a. |

**Redaction implementation details** (gemini-peer proposal refined by gamma + alpha):
- **Write-time for new rows, back-redact-at-render for pre-v3.1 rows (dual path).** Sanitize on write to SQLite (in `RedactingTransport`) going forward. For rows written before v3.1's migration (identified by `schema_version < 11`), apply back-redaction at render-time on the export path (and any future cloud-sync path). Rationale: write-back correction of historical rows risks false-positive corruption of legitimate prose; leaving history raw means pre-v3.1 secrets leak through export the moment it ships. Dual-path preserves historical DB integrity while guaranteeing *nothing unsanitized leaves the DB boundary*. Adds ~1 day to the redaction cost estimate.
  - **Resulting safety property:** "All rows leaving the DB (via export or future cloud sync) are redacted. In-place DB contents predating v3.1 are unchanged but never exposed unsanitized."
- **Performance budget: <2ms p99 on 8KB messages.** Redaction is a write-path interceptor. The v3.0 hook budget is <10ms; redaction must not consume that budget for sends. If the regex catalog grows past the budget, cut catalog size before degrading perf — a thinner catalog is a feature-scope problem; blown latency is a user-visible regression.
- **Regex-first, not entropy-first.** Known patterns: `sk-…`, `ghp_…`, `xoxb-…`, AWS `AKIA`, GitHub PATs, JWT shape (3-segment base64 dotted), PEM blocks, `.env`-shaped lines. Entropy detector only as a warn-not-redact signal reviewer can inspect — entropy-based detection misflags ~3-5% of legitimate data (UUIDs, git hashes, session keys).
- **Opt-out: per-message flag, not env-var.** `peer send --allow-secrets` forces conscious per-send choice. Do NOT use env-var-based opt-out like `AGENT_COLLAB_REDACTION=off` — env vars stick across sessions and get left on by accident in long-running agents. Sandbox/dev token sharing for coordination is a real workflow, but should require explicit per-message intent.
- **Auditable markers.** Replace with `<REDACTED_BY_PEER_INBOX:pattern=github_token:audit=NONCE>` — reviewers see *that* something was redacted and *what class* without seeing the value. **Critical: do NOT use a hash of the secret as the audit ID.** Short hashes of known-format-prefix tokens are brute-forceable — a 4-byte hash of a 40-char alphanumeric GitHub PAT with known `ghp_` prefix narrows the search space dramatically. Use a per-redaction random nonce, or derive from `(idempotency_key, byte_offset)`. The goal is "did something get eaten here?" not "is this the same secret as that one over there."
- **Scope disclaimer (v3.1 MVP).** This is a watered-down version of production redaction (AWS Macie, GitHub Secret Scanning are entire products). The 4-6d estimate is for MVP only — ~20 format catalog, baseline FP tuning, per-message opt-in, nonce audit, core adversarial tests. A hardening pass (expanded catalog, provider-specific FP tuning, redaction observability) is an explicit v3.2 or v3.3 consideration, not part of v3.1 scope.

### v3.2 — "Continuity + reliability" (~5-7 days)

- **Room-digest carryover across pair resets and fresh sessions** (gamma, 3-5d). When a pair hits the 100-turn cap or a session ends, write a compact digest (converged decisions, open questions, member roster, converged commitments — positions the room staked that downstream decisions depend on; **not** git commits) to a persistent store keyed by a *semantic room ID*. New sessions joining the same semantic room auto-load the digest at first-turn. Addresses the real continuity gap — gamma hit the 100-turn cap mid-roundtable; a pair reset today would have lost 9 rounds of convergence. Alpha endorsed ranking this ahead of the papercut fixes.
  - **Dogfood datapoint (2026-04-17):** the pair_key `dusty-island-8114` that produced this very roadmap hit the 100-turn cap mid-broadcast while welcoming a new joiner; owner manually reset + raised `AGENT_COLLAB_MAX_PAIR_TURNS=300`. The reset preserved SQLite rows but there's no current mechanism for a future session to inherit our 11 rounds of convergence automatically — this doc (written to `plans/`) is the only retained artifact. Raises this item's priority above a papercut: it's the difference between a roadmap that survives a cap-hit and one that forces an owner to paste context manually into every new session. `reason` elevated this as evidence on re-entry.
  - **Semantic room ID definition:** a stable string (user-supplied at `session register`, or auto-derived from a hash of founding-members + first-topic) that persists across `pair_key` rotations. When a new `pair_key` is minted for the same semantic continuation, sessions joining it inherit the existing digest. Distinct from `pair_key`, which is ephemeral. Open design exercise: exact derivation rule is a v3.2 spec decision.
- **Per-message delivery semantics (`delivery=durable`)** (gamma backlog, 2-3d post-v3.0 idempotency). Ack-or-escalate for load-bearing messages (mediator convergence broadcasts, coder session reports). Best-effort stays default for chatter.
  - **Escalation target — split by recipient agent type** (idle-birch finding): Claude recipients have `UserPromptSubmit` auto-inject available; inject unacked durable as `<peer-inbox urgent="1">` on next prompt. **Codex / Gemini recipients do NOT have auto-inject today** (per `docs/PEER-INBOX-GUIDE.md` runtime table — they require manual `peer receive`). For them the escalation is: persist the `urgent=true` flag on the inbox row, surface on next `peer receive` with a distinct prefix, and print a warning at `session register` time if urgent-unread exists. Equivalent hook paths for Codex/Gemini are out-of-scope until vendor tooling supports them. Mediator is NOT a hardcoded escalation target — room may not have one.
    > **Stale post-Topic-3 (2026-04-19, room-pi finding):** this paragraph predates Topic 3 Arch D + v0.3 pi SOFT SHIM. Option J already shipped hook-parity across claude/codex/gemini (tag `v3.x-option-j-shipped`), and v0.3 routes `--cli=codex` + `--cli=gemini` through spawnPi which inherits the same envelope-prompt instruction block (post-v3.1 [PROMPT] patch) that teaches CLIs how to emit peer-send replies. The "do NOT have auto-inject" framing conflates (a) interactive-mode hook inject vs (b) daemon-mode envelope-prompt delivery — post-Topic-3, path (b) exists for all three CLIs through the daemon. Full re-read needed when v3.2 scope-doc re-opens; see §14 meta-lesson trio + §14 closure-arc.

### v3.3 — (removed as a phase)

The bash-level git-diff metadata item was always conditional ("ships as interim if demand materializes before v4"). Round-2 review concluded: committing to a stopgap we'll then replace adds cost without moving the destination. The context-diff capability folds into v4's `SessionReport` typed payload natively — `SessionReport { files_changed, loc_delta, tests_passed }` is exactly the differential metadata block. Shipping the typed version once is cheaper than shipping bash stopgap + typed version.

### v4 — "Mechanize the synthesis role" (weeks, high blast radius)

- **Typed structured payloads with a registered message schema** (gamma, 1w protocol + ongoing adoption effort). Extend `Envelope` (already frozen in v3.0 with `type` + `Any payload` built for this) with typed messages for common collaboration primitives: `ReviewFinding`, `ConvergenceDecision`, `ActionItem`, `OpenQuestion`, `SessionReport`, `RoundConvergence`. Agents emit prose + structured data. Informed by what v3.1's export reveals about recurring shapes — don't formalize patterns you haven't observed. `SessionReport` carries the differential metadata (files_changed, LOC delta, tests_passed) that the removed v3.3 stopgap was going to deliver.
- **Git-aware `ReviewFinding` binding** (gamma backlog, 1-2w, depends on typed payloads). Review payloads carry `{file, line_range, severity, claim, evidence}`. Aggregatable like inline PR comments.
- **Automated synthesis dashboards** (gamma's real long-game). Shows open questions, pending reviews, convergence state — mechanized, not hand-synthesized. Mediator's current manual job becomes a SQL query.
  - **Dependency: `cmd_peer_web` migration.** The current admin dashboard is a Python HTTP handler in `scripts/peer-inbox-db.py` (function `cmd_peer_web`; was 453 LOC at brainstorm time; grew to ~942 LOC through v3.0 W1 and Topic 3 and continues to drift — use symbol search, not line number, per §13 retrospective note on systemic doc-citation rot), not ported in v3.0. v4 dashboard work picks one path, names it explicitly in its own spec: **(a)** keep Python `cmd_peer_web` alive alongside Go forever (dual-maintenance cost); **(b)** port to Go as a v4 prerequisite (~3-5d, adds to v4 scope); **(c)** build a Go-native dashboard from scratch on top of typed payloads (~1w, throws away Python UX but gives clean typed-payload-native foundation). Leaning (c) as the cleanest fit for v4's typed-payload substrate, but this is a v4 spec decision, not a v3.1 lock-in.
- **CI integration.** `ReviewFinding` with `severity=blocking` blocks merge.
- **Conversation forking / subthreads** (gamma backlog, 1w). Turn-budgeted subtopics inside a room with merge-back semantics.
- **Agent-capability matching for routing** (gamma backlog, 1w). "Find a peer with `agent=codex` and `role=reviewer`" instead of name-addressing.

## Principle that emerged

**"Don't structure data you haven't seen the shape of yet."** (alpha)

Export reveals the shape. Typed payloads then formalize the recurring patterns. Sequencing is the win, not picking a single killer. v3.1 ships what's already in the DB as safe artifacts; v4 formalizes the patterns those artifacts reveal.

## Rejected / deferred

- **Context-differential broadcasts as a standalone v3.1 item OR as a v3.3 bash-level stopgap.** Gemini-peer proposed 2d; gamma and alpha agreed it overlaps significantly with typed payloads (`SessionReport.files_changed` IS the differential metadata). **Resolution: folds into v4 `SessionReport` typed payload, no interim.** Shipping a bash stopgap + typed version later would cost more than shipping the typed version once; v3.3 is no longer a phase.
- **Entropy-based secret detection as primary signal.** False-positive rate too high (~3-5% on UUIDs/git-hashes). Regex-first, entropy as warn-only.
- **VoteCast / ActionItem as first typed payloads.** Unproven in actual rooms; ship `ReviewFinding` + `RoundConvergence` first (clear winners based on this session's patterns); validate others against real export data before adding.

## Meta-observation (worth preserving)

From gamma's final synthesis:

> *The room converged on a better answer than any single participant proposed. Alpha had export but underweighted security; gemini-peer had redaction but didn't explicitly pair it with export; I had typed payloads but missed the adoption-discipline trap. Synthesis produced the right bundle. This is exactly the mechanized-synthesis use case my killer was aimed at — we're the manual SQL query right now.*

This is both the evidence for gamma's v4 killer AND the evidence for alpha's principle that you can't type-system what you haven't observed. The manual mediator-synthesis we just did is the observation; v4 typed payloads is the formalization.

## Non-engagement

- **idle-birch** (codex, peer) — did not respond to the original brainstorm broadcast. *Update 2026-04-18: idle-birch subsequently surfaced the load-bearing hook-hijack catch during Topic 3 impl-scope review (daemon-spawn path-separation collapse at the hook boundary), and delivered substantive drift-check passes on this doc + `plans/v3.2-frontend-brainstorm.md` + `plans/v3.x-headless-delegation-brainstorm.md`. Crediting deferred-but-material contribution.*
- **joseph** — no-channel peer; did not respond.

## References

- Prior plan: `plans/v3.0-go-cloud-ready-scoping.md` — the v3.0 spec + implementation plan this roadmap layers on top of.
- Peer-inbox room: `dusty-island-8114` — full transcript is the primary reference for decision rationale (ironically unpreservable today in any usable shape, which is the case v3.1 item #2 `room export` solves). **Note for future readers:** if the SQLite DB has been rotated and the transcript is gone, the bundle tables above are intentionally self-contained — every decision is stated inline with its rationale so this doc stands alone even without the room contents.
- **Contributor credit (split by distinct contribution):**
  - **alpha:** `room export` (killer), stale-marker auto-reaper (honorable mention), session-key → label disambiguation (honorable mention), **redaction-at-write-time (vs at-export-time) sharpening**, **hash-as-audit-ID leak catch** (distinct from the timing refinement — caught a secondary vulnerability the initial auditable-marker proposal would have shipped), per-message-opt-in (vs env-var) for `--allow-secrets`, and the "don't structure data you haven't seen the shape of yet" sequencing principle.
  - **gemini-peer:** protocol-native secret redaction (killer — the safety gap that reframed export), context-differential broadcasts (honorable mention, reviewer token-context efficiency).
  - **gamma:** typed structured payloads (v4 long-game), room-digest carryover, per-message delivery semantics (`delivery=durable`), git-aware `ReviewFinding` binding, conversation forking / subthreads, agent-capability matching for routing, `peer status --pair-key` papercut, and the "room converged on a better answer than any single participant proposed" meta-observation.

## 13. Post-Topic-3-implementation retrospective (2026-04-18/19, live-reviewer concurrence)

> **Date-range update (2026-04-19):** the original §13 retrospective captured state after Option J shipped + Topic 3 v0 in flight. The arc continued rapidly through 2026-04-18 into 2026-04-19 with five additional Topic 3 closures (v0.1 / v0.1.2 / v0.2 / v0.2.1 / v0.3) plus v3.1 [PROMPT] + v3.1 recommended-models-table ride-alongs. §13.x below captures the post-v0.3 arc without disturbing the 2026-04-18 scaffold.

Added after Option J shipped (`v3.x-option-j-shipped`) + Topic 3 v0 implementation in flight (migrations `c96868f` + Python daemon-mode `9410604` + Go daemon-mode `0043eb0` + hook short-circuit `e15ccca`). Feeds back into this brainstorm without disturbing the historical scaffold. Five-way concurrence: alpha + gamma + reason + test-engineer + idle-birch.

### β-i / β-ii split (alpha, four-reviewer endorsed)

Within v3.1-β, items unblockable independent of redaction scope-doc ratification:

- **v3.1-β-i (papercut bundle, ~2d total, test-authorable immediately):**
  - #3 stale-marker auto-reaper — half-shipped at `712121c` (held local); ~0.5d to finish.
  - #4 session-key disambig — **concretely reinforced this session**: first `agent-collab peer list` invocation returned the exact "multiple sessions registered; pass --as <label>" error this item fixes. ~0.5d owner-UX win.
  - #5a `peer status --pair-key` cheap core — ~1d.
- **v3.1-β-ii (redaction + export, ~6-8d, gates v3.2-α):**
  - #1 redaction MVP (~4-6d), #2 export (~1-2d), with `room export` attached to redaction even under the split per idle-birch — the safety dependency is load-bearing (redaction-before-export prevents export becoming a leak vector).
  - β-ii needs scope-doc ratification before test-authoring can start (adversarial scaffolding, nonce-audit, per-message opt-in, catalog shape).

### δ-into-v3.2-papercuts scheduling option (test-engineer)

Given v3.2 papercuts + v3.2-γ (test-fixture inspector) are unblockable independent of v3.1 redaction per v3.2 re-read's four-way consensus, and v3.1-δ is ~1-2d of scattered cleanup, δ could merge into a unified "hygiene cluster" alongside v3.2 papercuts. Owner-scheduling option, not a scope shift. Mirrors β-i/ii logic at δ granularity.

Post-α-ship, the doc's original α→β→γ→δ rigid sequencing becomes looser: maintenance clusters (γ-remaining-work + δ + v3.2 papercuts + v3.2-γ) can batch with scheduling flexibility; β-ii redaction is the one sub-phase that retains hard-gate status (blocks v3.2-α specifically — not "v3.1 blocks v3.2" wholesale, per reason + idle-birch sharpening).

### Topic 3 / Option J carry-over patterns worth lifting into β redaction scope-doc

When v3.1-β-ii opens, these implementation-design patterns from Option J + Topic 3 transfer cleanly:

- **Five-guarantee cluster-naming discipline (Topic 3 §3.4)** — redaction has similar cluster shape (regex-first + entropy-warn + nonce-audit + per-message opt-in + dual-path write-vs-back-redact). Explicit guarantee-naming beats implementation-latent contracts.
- **CI-feasibility env-override idiom (`AGENT_COLLAB_SWEEP_TTL`)** — adopted in Topic 3 per test-engineer's ask (`9410604`). Redaction implementation should bake in: `AGENT_COLLAB_REDACTION_DISABLE=1` for fixture-mode tests where canned secrets are intentional; `AGENT_COLLAB_REDACTION_CATALOG=path/to/test-catalog.json` for catalog-injection in tests; potential `AGENT_COLLAB_REDACT_BUDGET_MS` for stressing <2ms p99 budget without production-surface impact.
- **Option J's env-flag short-circuit pattern (`AGENT_COLLAB_DAEMON_SPAWN=1`)** — additive, matches existing `AGENT_COLLAB_FORCE_PY=1` pattern. Template for `RedactingTransport` opt-out points at well-defined boundaries.
- **Coverage-axis vs visibility-axis methodology (alpha, HD retrospective)** — applies to redaction design: write-time (broad coverage, silent on false-positives) vs back-redact-at-render (narrower coverage, per-render observability). Explicit axis-tagging prevents the category error that happened in the Topic 3 round on receive_mode / SQL-partition ordering.

### Daemon-mode wrinkle on redaction blast surface

Topic 3 daemon-mode adds a new shape to v3.1-β: `claimed_at IS NOT NULL AND completed_at IS NULL` rows can sit for up to `TTL` (default 600s) before sweeper requeue. Until β redaction lands, in-flight claimed rows may contain unredacted secrets. Not a blocker (interactive rows have the same exposure today), but the claim-window-extends-exposure is a new wrinkle worth naming in β MVP scope. Export design should also distinguish daemon-spawn origins from interactive (e.g. `[daemon-spawn @ ts | from=label]` vs `[label @ ts]`) given `claim_owner` / `claimed_at` / `completed_at` columns now exist.

### γ cost narrowed (test-engineer concrete evidence)

Doc's original γ estimate of 2-3d assumed goose baseline + legacy port + migrate binary. Concrete audit shows baseline + migrate binary shipped (W1 `6568f8b` + Topic 3 `c96868f`); only legacy Python `init_db()` removal + `tests/migrate-upgrade-path.sh` remain. Realistic ~1-1.5d.

### Gate precision (reason + idle-birch sharpening)

"v3.1 blocks v3.2" in the original framing is too coarse. Precise statement: **v3.1-β redaction gates v3.2-α (Synthesis Workbench) specifically.** v3.2 papercuts (turn-cap warning, stale-session filter) and v3.2-γ (test-fixture inspector + post-mortem mode) are NOT redaction-gated per `plans/v3.2-frontend-brainstorm.md` §Safety gate structure (four-way consensus in that doc's re-read). Owner gets scheduling optionality.

### Gamma's pre-deferral α→γ→β→δ reorder challenge — weakened post-α-ship

Gamma's original reorder challenge (captured in §Challenges for re-opening fold) proposed γ-before-β to save 2-3d of β migration-complexity surface by landing clean schema baseline first. Post-α-ship, γ is maintenance-shaped (no downstream gating) and β is the urgent downstream gate for v3.2-α (safety-urgency). Reorder has weaker standing than pre-α-ship. Owner's call when v3.1 opens — weigh clean-baseline savings (γ first, 2-3d) vs v3.2-α unblock urgency (β first). Not withdrawn; just reduced-standing.

> Post-v0.3 re-check (2026-04-19): the rigid α→β→γ→δ serial has already dissolved — v3.1 ride-alongs (recommended-models, [PROMPT], v0.3 collapse) shipped out-of-sequence before v3.1-α opened. The reorder framing no longer has a serial to reorder; challenge architecturally superseded, not merely reduced-standing. Closed.

### Systemic doc-citation rot observation (alpha + cross-doc pattern)

The `scripts/peer-inbox-db.py:1829` / `~453 LOC` citation drifted through three brainstorm docs (this one + `plans/v3.2-frontend-brainstorm.md` + `plans/v3.x-headless-delegation-brainstorm.md`). Current: `:2448` / `~942 LOC` (and still mobile — grew by ~114 lines between mid-session and late-session re-reads, likely from coder's commits 4+6 landing). Systemic pattern from file growth through v3.0 W1 + Topic 3 work. Future doc citations should use symbol-keyed references (e.g. "search for `def cmd_peer_web`") not raw line numbers for any target that's actively being modified.

> Post-Topic-3 re-check (2026-04-19): file at 4163 LoC — 4.4x growth since observation; symbol-keyed-reference recommendation now load-bearing, not merely preferable.

### S3 orphan reminder (cross-doc)

Per HD retrospective: S3 (API-backed cost model) remains orphan; gemini-peer canonical-API-viability sub-dependency broken (session stale). Relevant to v3.1-β only insofar as API-adapter patterns reshape eventual Topic 2 scoping; not v3.1 blocking.

## 14. Post-Topic-3-v0.1-through-v0.3 closure arc (2026-04-18 → 2026-04-19)

Added 2026-04-19 after v3.x-topic-3-v0.3-shipped closed. Captures the
five-release arc that layered between the original §13 retrospective
(2026-04-18, Topic 3 v0 in-flight) and the ship of the pi-as-universal-
harness collapse. Aggregated reviewer inputs: coder (§14 author) +
room-codex + room-pi + room-pi-codex + room-pi-gemini + room-gemini +
test-engineer + mediator + gamma (post-sweep drift-check). Structural
preference: test-engineer advocated §14 append over §13 continuation to
keep the 2026-04-18 retrospective scaffold clean; honored.

### 14.1 Closure stack (tags on origin/main)

The ladder has extended by five ships since §13 was written:

```
v3.x-option-j-shipped         (b5f9485)  Option J hook-delivery baseline (covered in §13 above)
v3.x-topic-3-v0-shipped       (ab09f81)  Arch B auto-reply daemon + §3.4 contract
v3.x-topic-3-v0.1-shipped     (371609b)  Arch D codex+gemini capture/resume; claude asymmetry documented
v3.x-topic-3-v0.1.2-shipped   (6ec4b23)  Arch D EMPIRICALLY FUNCTIONAL — real-CLI bug fixes
v3.x-topic-3-v0.2-shipped     (ab61c24)  pi as 4th Arch D CLI — path-as-identity
v3.x-topic-3-v0.2.1-shipped   (b66476a)  zai-glm plugin-env-var correction
v3.x-topic-3-v0.3-shipped     (4d65bf3)  pi-as-universal-harness collapse — codex/gemini SOFT SHIM
```

Plus two v3.1-labeled ride-alongs on origin/main (not separately tagged):
- `23da0c1` — v3.1 [PROMPT] patch: envelope-prompt interpolation + reply-path + retire mechanism-1 peer-inbox-daemon-complete language.
- `16d0ff1` — v3.1 recommended-models-by-provider canonical table in operator guide.

**Name-collision note:** the v3.1 label in this doc's sub-phase
proposal (α/β/γ/δ) refers to the Option-J-through-redaction work.
"v3.1 [PROMPT] patch" + "v3.1 recommended-models table" are operator-
ergonomics hygiene ride-alongs that happened to share the major
version label. Future references should disambiguate via SHA or
phrase-prefix ("v3.1-α Option J" vs "v3.1 [PROMPT]" vs "v3.1
recommended-models").

### 14.2 M1-M7 — closure-arc entries

**M1. Topic 3 v0.1 @ `371609b`.** Arch D codex + gemini capture/resume
+ claude asymmetry (§4.3). First ship of CLI-native session-resume
pass-through; opt-in via `--cli-session-resume`. Retired in v0.3
alongside codex-direct + gemini-direct per SOFT SHIM.

**M2. Topic 3 v0.1.2 @ `6ec4b23`.** Real-CLI E5/E6 probes surfaced two
operational bugs post-v0.1 ship: (BUG 1) codex 0.121.0 emits session-
id banner to STDERR not stdout; v0.1 scanned stdout only. (BUG 2)
gemini 0.38.2 `--list-sessions` takes ~5.3s wall-clock; v0.1 hardcoded
5s timeout missed deterministically. Fixed + bundled + re-probed.
Meta-lesson captured in §14.3 (L1).

**M3. Topic 3 v0.2 @ `ab61c24`.** Pi as 4th Arch D CLI with daemon-
owned session-file PATH (`sessions.daemon_cli_session_id` reused for
path values). §3.4 invariant 5 "re-create-at-same-path" — missing file
on disk is tolerated by pi's idempotent `--session <path>` contract.
8 CI gates + E8 real-CLI probe PASS (pi 0.67.68 + ZAI GLM). Reset verb
file-delete gated on `sessions.agent == 'pi'` + `os.Remove` NotExist
tolerance. First path-as-identity model in the repo.

**M4. Topic 3 v0.2.1 @ `b66476a`.** Mediator empirical probe post-v0.2
ship caught a regression in v2 env-var absorption: room-codex reported
`ZAI_API_KEY` for zai-glm based on `pi --help` reading, but zai-glm is
a third-party pi-mono plugin (`pi-zai-glm`) that reads
`ZAI_GLM_API_KEY`. v2's "fix" was actually a regression; v0.2.1
reverted + split the provider-auth env-var table into built-in
(enumerated in `pi --help`) vs plugin (invisible to `--help`; verify
via plugin registerProvider() source). Meta-lesson captured in §14.3 (L2).

**M5. Topic 3 v0.3 @ `4d65bf3`** (TAG `v3.x-topic-3-v0.3-shipped`).
Pi-as-universal-harness collapse: codex-direct + gemini-direct
RETIRED; SOFT SHIM routes `--cli=codex` → pi-openai-codex and
`--cli=gemini` → pi-google-antigravity. ~1690 net LoC removed from
daemon code (spawnCodex + spawnGemini + 8 capture helpers + 3 package-
level regex vars deleted). E10 BOTH phases PASS against live pi-mono
0.67.68 + openai-codex OAuth + google-antigravity OAuth. Claude-direct
preserved (OPTION A partial per v0.1 §4.3 asymmetry); v0.4 HARD RETIRE
scheduled for the SOFT SHIM flag acceptance. Shape β WIDE agent-gate
(`sessions.agent IN {pi, codex, gemini}`) + path-shape guard
(`strings.Contains(cached, "/")`) implemented in reset verb (Go +
Python parity) — preserves v0.2 4e/5h cross-CLI-reset-isolation
invariants while covering shim-backed rows.

**M6. v3.1 [PROMPT] patch @ `23da0c1`** (shipped 2026-04-18). Three
envelope-prompt defects dogfooded-caught + fixed:
1. Literal `<YOUR_CWD>` / `<YOUR_LABEL>` placeholders replaced with
   spawn-time `fmt.Sprintf` interpolation of `cfg.Label` + `cfg.CWD`.
2. Mechanism-1 `peer-inbox daemon-complete` block retired from the
   envelope prompt (the binary was never installed by the protocol's
   install script; all three dogfoods used mechanism-2 de facto).
3. Reply-path instruction block appended teaching CLIs how to emit
   `agent-collab peer send --as <MY_LABEL> --to <THEIR_LABEL> --message ...`
   with MY_LABEL interpolated + THEIR_LABEL templated for multi-sender
   batches. E9 probe 2026-04-19 PASS 4/5 peers (room-gemini direct hit
   ack_timeout_abandoned consistent with pre-v0.3 behavioral quirk —
   not a v3.1 regression; the gemini-direct path itself is now retired
   in v0.3). Meta-lesson captured in §14.3 (L3).

**M7. v3.1 recommended-models-by-provider table @ `16d0ff1`** (shipped
2026-04-18). Canonical single-source-of-truth table in
`docs/DAEMON-OPERATOR-GUIDE.md` § Architecture D → Model selection.
Maps provider → recommended-model → auth-source → validated-by-probe-
pointer. Covers `openai-codex` (gpt-5.3-codex / OAuth / E10a), `google-
antigravity` (gemini-3-flash / OAuth / E10b), `zai-glm` plugin (glm-
4.6 / ZAI_GLM_API_KEY / E8), `anthropic` (not yet probed; v0.4+
scoping). Daemon does NOT enforce a default per v0.3 §4.2 — operator
MUST supply `pi.model`; this table is the operator-facing "here's what
to put" reference.

### 14.3 Meta-lesson trio — explicit (test-engineer)

Three durable project-wide rules crystallized during this arc. Name
them explicitly alongside alpha's earlier "don't structure data you
haven't seen the shape of yet" principle:

- **L1 — Real-CLI probe required for Arch D-family closure (v0.1.2).**
  *Fake-binary gates don't validate real-CLI behavior.* v0.1 shipped
  with fake-binary gates passing; real-CLI E5/E6 probes surfaced two
  operationally-critical bugs (codex stderr-capture, gemini 5s
  timeout). Every Arch D-family scope-doc §11 closure block now
  mandates at least one real-vendor probe. Annotated at v0.1.2 +
  v0.2 + v0.3 §11. Extends alpha's "observe shape first" principle
  with a complementary "validate against reality last."

- **L2 — Plugin-provider env vars need source-read not --help-read
  (v0.2.1).** `pi --help` is authoritative for built-in providers
  only. Third-party pi plugins (e.g. `pi-zai-glm`) register via
  `pi.registerProvider()` with their own env-var names that `pi
  --help` doesn't enumerate. For plugin providers the verification
  protocol is reading the plugin's source directly; `--help`-only
  audits are structurally incomplete. Annotated at v0.2.1 scope-doc
  §10.1.1 + operator guide Arch D § Pi env-var table.

- **L3 — Patch-merged ≠ patch-deployed; daemons are deployment not
  code (v3.1 [PROMPT]).** v3.1 E9 round-1 probe surfaced this: the
  [PROMPT] patch landed on origin/main at `23da0c1` but the four
  daemon-backed room peers were still running their pre-patch
  binaries. Re-starting the daemons was a required deployment step
  that a naive "did the commit merge?" check missed. Daemons are a
  deployment surface with their own lifecycle distinct from source
  state; CI green + merged does not imply operational deployment of
  behavior. Annotated at v3.1 E9 last-run (docs/DAEMON-CLI-SESSION-
  VALIDATION.md).

### 14.4 E-probe last-run ladder (test-engineer)

Real-CLI probe records live in `docs/DAEMON-CLI-SESSION-VALIDATION.md`
§ Last-run. Current ladder (newest first):

| Probe | Tag anchor                        | SHA       | Date       | Outcome                          |
|-------|-----------------------------------|-----------|------------|----------------------------------|
| E10   | `v3.x-topic-3-v0.3-shipped`       | `4d65bf3` | 2026-04-19 | PASS (10a openai-codex + 10b google-antigravity both phases) |
| E9    | v3.1 [PROMPT] patch               | `23da0c1` | 2026-04-19 | PASS 4/5 (room-gemini ack_timeout_abandoned — pre-v0.3 quirk, now retired) |
| E8 (re-probe) | `v3.x-topic-3-v0.2.1-shipped` | `b66476a` | 2026-04-18 | PASS (ZAI_GLM_API_KEY-only revert confirmation) |
| E8    | `v3.x-topic-3-v0.2-shipped`       | `ab61c24` | 2026-04-18 | PASS (pi 0.67.68 + ZAI GLM end-to-end) |
| E5/E6/E7 | `v3.x-topic-3-v0.1.2-shipped`  | `6ec4b23` | 2026-04-18 | PASS (codex 0.121.0 + gemini 0.38.2 — RETIRED probes post-v0.3) |

E5 + E6 preserved under RETIRED banners in validation doc (test-
engineer F4 preservation pattern) so v0.1-era tag annotations remain
auditable even though the underlying code paths are deleted.

### 14.5 Room-peer evidence signals (live-dogfood observations)

The Topic 3 v0.2+ arc has produced **daemon-backed room peers** as a
new artifact class — claude/codex/gemini/pi peers reviewing
implementation state from inside the very spawn the implementation
ships. This is EVIDENCE (directly observable at dogfood time), not
ORAL HISTORY. Representative signals this arc:

- **room-codex** (pi+openai-codex via SOFT SHIM) self-introspection
  2026-04-18T16:34Z during coder↔room-codex live dogfood: "my parent
  process is `pi` (PPID command shows `pi`); env includes
  `PI_CODING_AGENT=true` + `AGENT_COLLAB_DAEMON_SPAWN=1`; daemon log
  shows `daemon.spawn.exec` argv = `pi --provider openai-codex
  --model gpt-5.3-codex --session ~/.agent-collab/pi-sessions/room-codex.jsonl
  -p <prompt>` + `daemon.cli_collapse.v03_shim` warning". Three
  independent evidence sources (process tree + env vars + daemon log)
  confirmed SOFT SHIM routing end-to-end. The room-peer architecture
  means post-ship dogfood is **not just operator testing** — it's the
  shipped code reviewing itself with first-person knowledge of its own
  runtime environment.

- **room-pi** (pi+zai-glm native) confirmed post-v0.3 that the SOFT
  SHIM adds zero spawnPi complexity; 3-provider dogfood
  (zai-glm + openai-codex + google-antigravity) validates path-as-
  identity uniformly across providers.

This shape didn't exist before v0.2 and is under-documented elsewhere
in the roadmap. Future v3.1-β redaction review rounds + v4 typed-
payload adoption work can leverage daemon-backed room peers as
live-context reviewers rather than session-spawning-on-every-round.

### 14.6 v3.1 papercut queue (mediator-tracked, as-of 2026-04-19)

Reset the status from the original §13 version. Current items grouped
by category where topically linked (owner-surfaced session-lifecycle
cluster; discrete items remain numbered):

**Session-lifecycle cluster (owner-surfaced 2026-04-19 — interlinked):**

- **P8 [ROOM-ACCESS] Silent room-access loss.** Agents age out of
  peer-inbox rooms without an operator-visible signal; gamma + reason
  + alpha all aged out mid-session today and owner only learned by
  attempting DMs that bounced. Needs one or both: (a) auto-heartbeat
  refresh from active sessions (hook-based or daemon-based); (b)
  owner-visible "peer went stale" surface (new `peer status` column,
  broadcast alert at threshold, or stale-marker reap with announcement).
  Candidate mechanism: extend the daemon-mode heartbeat surface (v4
  papercut `712121c` adjacent) to cover interactive-hook sessions too.

- **P9 [ROOM-ACCESS] Join-broadcast amplification.** On re-register,
  the `[system] <label> joined the room` event fires ONCE PER ROOM
  MEMBER. In today's 13-peer room = 13 duplicate broadcasts per re-
  join. Inbox clutter + observability cost. Fix options: (a) de-dup
  at broadcast-emit time; (b) collapse `joined` announcements to a
  single room-wide row. P9 is the visible-to-owner symptom of the
  underlying P8 access-loss churn — addressing P8 alone doesn't
  eliminate P9's amplification surface.

  Owner quote (2026-04-19): *"we have issues with agents losing room
  access."*

**Discrete items:**

- **P1 [ENV] pi provider-auth startup-validation.** Coder/test-engineer
  shape-disagreement from v0.2.1 arc; unresolved. Options: (a) fail-
  fast at daemon startup when env var expected by pi.provider is
  missing (requires daemon to know plugin-provider env-var mappings,
  which `pi --help` alone doesn't expose per §14.3 L2 meta-lesson);
  (b) operator-docs-only recommendation with shell-agnostic framing
  matching v0.1 §6.2 GEMINI_CONFIG_DIR precedent. Owner-schedulable.

- **P2 [PROMPT-DEBUG] envelope prompt doesn't name CLI routing path.**
  NEW 2026-04-18/19 from coder↔room-codex dogfood. room-codex correctly
  confirmed SOFT SHIM routing but had to introspect process tree +
  daemon log; the envelope prompt itself doesn't mention "--cli=<X>
  (SOFT SHIM → pi-<Y>)". Would add operator-debuggability-through-
  envelope-only — small addition (~5 LoC in `buildStaticPrompt`) that
  makes the shim self-describing from inside the spawn. Trivial ride-
  along; no blocker.

- **P3 [INTEGRATION-BEHAVIOR] gemini-3-flash OOB inbox-DB reconstruction
  post-reset.** NEW 2026-04-19 from E10b probe. Model retrieves pre-
  reset context via inbox-table read rather than session-file read.
  NOT a daemon regression (Arch D reset contract intact; fresh session
  UUID confirms); behavioral property of gemini-3-flash under current
  tool-access surface. Operator-guide clarification addition: "reset
  deletes the session file + NULLs the column, but model may still
  recall context from other reachable stores (inbox DB, OS
  clipboard, etc.) — reset is NOT a full context-wipe."

- **P4 reason §6 L2-bullet-1 annotation.** Carried from v0.1 handoff;
  reason not live at v0.3 challenge round. Re-route when reason is
  next active.

- **P5 smoke.sh macOS rm-on-non-empty fragility.** Pre-existing; noisy
  on macOS `rm` non-empty errors. Candidate fix: `chmod -R u+w
  "$TMP_ROOT"` before `rm -rf`, or set `GOMODCACHE` to separate dir.

- **P6 hook-latency-exec p99 flake.** Occasional over-budget p99 when
  run back-to-back after heavy SQLite gates. Candidate fix: add 5s
  settle-delay OR document "run this gate in isolation" in test-
  runner guidance.

**Pruned from earlier queue:**

- ~~P7 reason #3 codex mid-run UUID rotation~~ — **OBSOLETE POST-V0.3.**
  Codex-direct is retired; the UUID-rotation code path (spawnCodex +
  codexBannerSessionRE + codexStaleSessionRE + postSpawnCodexResumeHandling)
  is deleted. Queue entry retired alongside the code.

### 14.7 β-i audit correction + β-ii simplification post-v0.3 (test-engineer)

**β-i audit correction.** The earlier "v3.1-β UNSHIPPED" bullet at the
top of this doc claimed `712121c` was a half-shipped reaper-impl held
local. That's wrong on both counts: `712121c` is a `docs(plans)`
papercut-entry commit (doc-only) and it was pushed. Inline correction
landed in the earlier bullet. Items #4 (session-key disambig) + #5a
(`peer status --pair-key` cheap core) also have no in-tree ship
evidence. β-i ~2d estimate stands.

**β-ii redaction MVP simplification post-collapse.** v0.3's codex +
gemini → pi SOFT SHIM reduces the per-CLI write-path surface that β-
ii's redaction MVP has to cover:

- **Before v0.3:** 4 per-CLI branches (claude + codex + gemini + pi)
  each needed independent redaction wiring across envelope render +
  stored-row sanitize + per-vendor argv.
- **After v0.3:** 2 branches (claude-direct + pi-universal). Shim-
  backed rows inherit pi-universal redaction policy automatically
  (no per-CLI branching needed for codex/gemini post-shim).
- **Envelope render path:** single `buildStaticPrompt` + envelope
  render per spawnPi — no per-CLI fan-out to maintain.

Estimate trends to 4d-end of the original 4-6d range (closer to 4d
than 6d). β-ii gate on v3.2-α **unchanged** — redaction is still the
load-bearing safety gate v3.2 Synthesis Workbench depends on.

### 14.8 Roadmap ladder — Topic 3 arc placement + open architectural questions

**Topic 3 arc slot.** The original v3.1-α/β/γ/δ phase ladder frames
Option J hook-expansion / redaction / migration-cleanup / papercuts;
Topic 3 is a parallel track (daemon-mode operator ergonomics + Arch D
session-resume) that shipped between the original brainstorm and now.
Place the Topic 3 arc explicitly:

```
v3.1-α (Option J, SHIPPED b5f9485)
  ├── Topic 3 v0 → v0.1 → v0.1.2 → v0.2 → v0.2.1 → v0.3 (all SHIPPED)
  ├── v3.1 [PROMPT] patch (SHIPPED 23da0c1)
  └── v3.1 recommended-models table (SHIPPED 16d0ff1)
v3.1-β (redaction + export, PENDING; β-i papercuts available independent)
v3.1-γ (goose-baseline + legacy-init_db() removal, PARTIAL; ~1-1.5d remaining)
v3.1-δ (scattered small items + B3, PARTIAL)
v0.4 HARD RETIRE (NEXT in Topic 3 arc; removes SOFT SHIM flag acceptance;
  sweeps sessions.agent='codex'|'gemini' rows; deletes retired-banner stubs
  tests/daemon-cli-resume-codex.sh + tests/daemon-cli-resume-gemini.sh)
v3.2 (Continuity + reliability, PENDING; gate per §13 precision)
...
```

**v0.4 HARD RETIRE — locked follow-on to v0.3.** Per v0.3 scope-doc
§11 + §10 Q6 ratification. Single-release migration window for
operators; scoping round opens when owner ratifies.

**Claude-via-pi-anthropic — open v0.4+ architectural question.** v0.3
OPTION A preserved claude-direct per §4.3 (coder pre-draft spot-probe
found `~/.pi/agent/auth.json` lacks anthropic OAuth by default;
collapsing claude would force operators into a credential-provenance
migration separate from the codex/gemini collapse story). Upgrade path
exists (claude-via-pi-anthropic GAINS cross-spawn context that v0.1
§4.3 asymmetry documented as missing) but blocked on owner auth-
provenance evaluation. **Not coupled to v0.4 HARD RETIRE timing.**

### 14.9 Open questions routed from reviewer returns

**Q1 (room-pi, 2026-04-19):** should v3.1-δ add a daemon-mode
papercuts cluster before HARD RETIRE? Daemon-mode papercuts currently
live in P1-P3 + scattered elsewhere; a bundled-cluster ship could
batch operator-facing polish in a focused window rather than
trickling. Owner-scheduling; no resolution here.

**Q2 (room-pi, 2026-04-19):** v3.2 §Per-message delivery semantics
line 117 (`AGENT_COLLAB_DAEMON_SPAWN=1` paragraph) predates Topic 3
Arch D + pi SOFT SHIM — the "Codex / Gemini recipients do NOT have
auto-inject today" framing is partially stale (daemon-mode + hook
parity shipped; pi SOFT SHIM covers codex/gemini as first-class
recipients for the daemon flows). Inline note or cross-ref added to
that section per room-pi finding; v3.2 scope-doc may need a full re-
read when it re-opens.

### 14.10 Doc-citation rot — compounding (cross-reference)

§13 "Systemic doc-citation rot observation" gained a one-line refresh
above (gamma's 2026-04-19 append; 4163 LoC = 4.4x growth since
original observation; symbol-keyed-reference recommendation now load-
bearing not merely preferable). Full observation stays in §13 for
attribution continuity; this §14 entry is a cross-pointer.

### 14.11 α→β→γ→δ reorder challenge — closed (cross-reference)

§13 "Gamma's pre-deferral α→γ→β→δ reorder challenge" gained a one-
line closure append above (gamma's 2026-04-19 close: "rigid serial has
already dissolved — v3.1 ride-alongs shipped out-of-sequence before
v3.1-α opened; architecturally superseded"). Owner no longer weighs
reorder tradeoff when v3.1-β/γ/δ open — the serial frame is gone;
sub-phases are schedulable independently per reason + idle-birch
sharpening + this closure.

### 14.12 Re-entry acknowledgments

- **gamma** re-registered mid-sweep 2026-04-18T16:39Z; mediator routed
  to drift-check lane. Delivered 2 closure appends (§13 doc-citation
  rot refresh + §13 α→γ→β→δ reorder challenge close) folded in
  place. Standing by for post-commit drift-check on this §14 draft
  covering: stale citations (symbol-keyed check), unimplemented-
  described contracts, v0.4 HARD RETIRE coverage, claude-via-pi-
  anthropic open-question framing.
- **reason** still stale; P4 carry-forward unchanged.
- **room-codex + room-pi + room-pi-codex + room-pi-gemini + room-gemini**
  all live at §14 authorship time; evidence-signal shape captured in
  §14.5.
- **test-engineer** structural preference (§14 vs §13.x) honored; β-i
  audit correction + β-ii simplification + meta-lesson trio + E-probe
  ladder all folded (§14.3, §14.4, §14.7).
