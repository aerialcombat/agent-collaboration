# v3.0 W1 Handoff — CLOSED

**Status:** W1 closed 2026-04-18 at tag `v3.0-W1` (commits `d06a198..2725901`, 11 commits, local `main` + `origin/main` in sync). Four-way reviewer [[verified-A]]: reason / gamma / alpha / test-engineer. · **Session:** dusty-island-8114

Load this doc + the three plan docs below to resume. You don't need the full transcript.

## Plan docs (required reading, in order)

1. `plans/v3.0-go-cloud-ready-scoping.md` — v3.0 spec + implementation plan. Converged across 9 rounds + 2 challenge rounds. **This is the authoritative plan.** Has the DoD, the implementation phases, the locked constraints.
2. `plans/v3.1-roadmap-brainstorm.md` — beyond-v3.0 roadmap (export, redaction, papercuts, typed payloads, room-digest carryover). Two rounds of challenge applied.
3. `plans/v3.2-frontend-brainstorm.md` — dashboard/frontend work. Six-reviewer convergence; mediator-ruling on the v3.1→v3.2 sequencing gate.

## Current state (2026-04-18, post-coder-round-2-broadcast)

### v3.0 W1 — SHIPPED, awaiting round 2 review

Coder completed the 14-item post-A3 queue and broadcast session-complete. Full test sweep green. **No code work in flight.** Coder is standing down per pause directive.

**Pause directive is ACTIVE:**
- All reviewers (alpha, gamma, reason, gemini-peer, idle-birch, test-engineer): standing down. No pre-review.
- Coder: standing down post-broadcast. Do NOT start W2.
- Mediator: reported to owner; holding for owner's go-signal before opening round 2.
- Owner: calls round 2 on their schedule.

**Reason for pause:** context management across a very long session. Pause is deliberate, not a wind-down.

### What coder shipped (confirmed green)

**A3 resolution (goose rework, mediator-ratified coder's chosen path):**
- `migrations/0001_reserve_v2_columns.sql` deleted.
- New per-engine files: `migrations/sqlite/0001_reserve_v2_columns.sql` + `migrations/postgres/0001_reserve_v2_columns.sql`, both with goose markers. SQLite uses bare ALTERs (safe because `goose_db_version` prevents re-apply). Postgres uses native `ADD COLUMN IF NOT EXISTS`.
- New binary `go/cmd/migrate/main.go` (~115 LOC) wrapping `pressly/goose/v3`. Flags: `-driver sqlite|pgx -dsn ... -dir ... up|down|status|version`. Builds to `go/bin/peer-inbox-migrate` (~19 MB with pgx; `modernc.org/sqlite` stays).
- `scripts/peer-inbox-db.py::migrate_v2_reserve()` deleted. `check_schema_version()` now reads `goose_db_version` (authoritative). `open_db()` auto-invokes the migrate binary when `goose_db_version < 1` or missing. Binary resolution: `shutil.which` → `~/.local/bin/peer-inbox-migrate` → `$REPO/go/bin/peer-inbox-migrate`. Missing-binary errors with install hint.
- `go/pkg/store/sqlite/sqlite.go::checkSchemaVersion` switched to `goose_db_version`; constant renamed `GooseVersionRequired = 1`.

**N3 (gamma's migration atomicity gap):** structurally no longer reachable. Goose wraps each migration in a transaction + tracks applied state. Verified empirically via T3.

**All queued non-blockers applied:**
- **B-1** (round-1, still green): Go returns `exitFallbackWanted=2`; bash falls back; T3 gates it.
- **N2**: `docs/protocol/v1.md:89` updated to "string with documented valid values (see v1.proto). Enum conversion deferred to v2.proto under the additive-only discipline."
- **A1**: `hookBlockBudget` now `hookBlockBudgetBytes()` reading `AGENT_COLLAB_HOOK_BLOCK_BUDGET`. Matches Python semantics.
- **A2**: `hooks/peer-inbox-inject.sh:74` ack_hash keys on `sha256(cwd + "::" + session_key)[:16]`. Deviation from plan: coder used `session_key`, not `label`. See deviations section below.
- **idle-birch #2**: `store.ErrPathDrift` sentinel added. `readMarker` returns `(Session, error)`, enforces marker-owner vs recorded-cwd match via `filepath.EvalSymlinks`. `ResolveSelf` propagates `ErrPathDrift`.
- **idle-birch #3**: Python `format_hook_block` `used` counter uses `len(entry.encode('utf-8'))` (bytes, matching Go).

**CI workflows:**
- `.github/workflows/schema-portability.yml` rewritten: invokes `peer-inbox-migrate -driver pgx -dir migrations/postgres up` against `postgres:16`. Asserts `goose_db_version.version_id = 1`, `meta.schema_version = 10` (legacy-compat), all four v2-reserve columns present. Re-apply idempotency tested.
- `.github/workflows/hook-latency.yml`: T1 + T4 steps enabled. T2 budget stays at 15ms. T3 was already enabled.

### Test results (all green as of last broadcast)

| gate | file | status | evidence |
|---|---|---|---|
| smoke | `tests/smoke.sh` | PASS | 50+ scenarios |
| T1 ack-collision | `tests/ack-collision.sh` | PASS | was FAIL pre-A2 |
| T2 hook-latency-exec | `tests/hook-latency-exec.sh` | PASS | p99=13.13ms, p95=11.22ms, cold=411ms |
| T3 fail-open-fallback | `tests/fail-open-fallback.sh` | PASS | gates B-1 |
| T4 utf8-budget-parity | `tests/utf8-budget-parity.sh` | PASS | Go/Python both 183 bytes after fix; was FAIL pre-bytes fix |
| in-process Go bench | `go/cmd/hook/hook_bench_test.go` | PASS | p99=2.13ms |

### Deviations coder flagged (for round 2 to ratify or request revert)

1. **A2 ack key uses `session_key`, not `label`.** Plan said `sha256(cwd + "::" + label)`; coder shipped `sha256(cwd + "::" + session_key)[:16]`. Session_key is more collision-resistant (random per-session) and is already the identity primitive elsewhere. Probably an improvement; owner/reviewer ratification needed.

2. **T3 fixture updated.** Coder updated `tests/fail-open-fallback.sh` (authored by test-engineer) because the new `checkSchemaVersion` reads `goose_db_version`, so the original drift trigger (drop `meta`) no longer exercises the fail-open path. Updated trigger drops `goose_db_version` + dependent indexes + v2-reserve columns; assertion 3 now checks `goose_db_version = 1` instead of `schema_version = 10`. Test semantics preserved, fixture mechanics adapted. Touches test-engineer's authored lane; coder offered to revert + DM diff for test-engineer to apply instead. Round 2 to decide.

### Notes coder surfaced (awareness, not deviations)

1. **Migrate binary size: ~19 MB** (vs hook's ~10 MB). Goose + pgx adds ~9 MB. Coder offered a conditional `nopostgres` build tag to drop pgx for SQLite-only installs. Owner decision.
2. **`meta.schema_version = 10`** seeded for back-compat only; no longer authoritative. `goose_db_version` is the source of truth. External code inspecting `meta.schema_version` sees `'10'` but internal code reads goose exclusively. Dual-tracking is deliberate, not technical debt.
3. **SQLite `DROP COLUMN`** requires index-drop-first — fixture gotcha, not a code issue.

### Decision points pending for round 2 / owner

1. **Ratify A2 deviation** (session_key vs label) or request revert.
2. **Ratify T3 fixture update** (coder-touched test-engineer's lane) or revert + test-engineer re-applies.
3. **Migrate binary size** — accept 19 MB, or build with `nopostgres` tag for SQLite-only installs? Also affects install-global-protocol distribution.
4. **Explicit W1 closure** — mediator adds "W1 phase closed" marker to `plans/v3.0-go-cloud-ready-scoping.md` after round 2 ratifies.

### Room state

- **Pair key:** `dusty-island-8114`
- **Turn cap:** raised to 500 (reset twice during the session: 100 → 300 → reset + → 500 + reset)
- **Members:** 15 registered (many stale). Active this session: alpha, gamma, coder, reason, test-engineer, gemini-peer, idle-birch (silent observer through brainstorm), mediator.

## Roles (binding protocol)

- **`coder`** (claude) — **sole implementer.** Only coder writes Go/Python/shell/CI/migrations/protobuf. Single-committer discipline is non-negotiable.
- **`mediator`** (claude, me) — only edits plan docs under `plans/`. No code edits. Routes messages, synthesizes review, arbitrates, ratifies rulings.
- **`alpha`, `gamma`** (both claude) — general reviewers. Substance + sequencing.
- **`reason`** (claude) — drift-checker. Watches for (1) scope re-opening, (2) speculative structuring, (3) abstraction drift from DoD evidence, (4) "rewrite in Next.js" scope fantasy, (5) reviewers collapsing mediator's arbitration step with authority-language like "MUST" or "LOCKED."
- **`idle-birch`** (codex) — code-grounded reviewer. File:line citations, verifies against current tree.
- **`gemini-peer`** (gemini) — UX/visual + safety lens.
- **`test-engineer`** (claude) — writes TEST code only (`.sh`, `*_test.go`, fixtures). Cannot edit base code or CI YAML (CI stays with coder). Scope explicitly includes empirical validation of reviewer claims (precedent: T2 shifted a DoD acceptance call).
- **Owner** (DJ) — final authority on locked constraints and scope.

**Reviewers can READ files freely. They CANNOT edit, write, commit, or patch.** Test-engineer can write test files only. If a reviewer thinks code must change, they describe it in a finding with file:line + rationale + suggested fix. Coder applies.

## Critical context not in the plan docs

### Load-bearing rulings (mediator)

- **v3.1 write-time redaction is a HARD GATE on v3.2 dashboard expansion.** Four-reviewer concurrence + localhost-exposure reasoning. Ratified by mediator, not by reviewer declaration. See `plans/v3.2-frontend-brainstorm.md` "Safety gate" section.
- **N1/N2 in v1.proto stay (b) hold-the-line.** Timestamp-type inconsistency in `InboxMessage.created_at` (string) vs `SessionRegister.started_at` (Timestamp) defers to v2.proto breaking-change protocol. `Error.code` stays `string`; `docs/protocol/v1.md:89` has doc-only wording update (coder applied). Do NOT change v1.proto field types.
- **W1 DoD revised to <15ms p99 real-exec**, not <10ms. Plan doc lines 13, 74, 152, 166, 178, 270 already amended. CI enforces via `tests/hook-latency-exec.sh`. Current reality: p99=13.13ms.
- **Dashboard narrow identity: "live review-coordination surface."** NOT transcript browser, NOT orchestration console, NOT code viewer. v3.1 room export handles durable history; mediator plays orchestration in-room; editor+LSP wins for code.
- **Data model must be mediator-agnostic.** Findings schema works for 2-peer lead/challenger rooms too, not just mediated rounds.
- **Procedural rule:** reviewer concurrence is input to mediator ruling, not self-ratifying. Reviewers should not use "MUST" / "LOCKED" / "governance-locked" framings.
- **A3 chose path (a):** coder reverts to goose. Three-reviewer concurrence + owner delegation to room. Mediator ratified. Shipped.

### What round 2 reviewers should verify (when owner opens round 2)

- All 14 queue items applied cleanly (see "What coder shipped" above).
- All six test gates still green (see table above).
- A2 deviation (session_key over label) — ratify or revert.
- T3 fixture deviation — ratify or test-engineer re-applies.
- Migrate binary size — accept 19 MB or request `nopostgres` build-tag path.
- Spot-check idle-birch #2 (`store.ErrPathDrift` semantics match Python's marker-owner check at `scripts/peer-inbox-db.py:915-933,954-960`).
- Spot-check goose N3 coverage (kill mid-migration, verify recovery).

### Non-blockers deferred to W2/later (not part of the 14-item queue)

- `cmd_peer_web` 453-LOC inline handler — v3.2 territory per frontend brainstorm. Gated on v3.1 redaction first.
- Python `scripts/peer-inbox-db.py` DRY cleanup at :1007/1792, :1101/1103, :1703-1722, :1330-1344 + :1456-1470 — plan doc W4 track.
- `poc/` deletion — plan doc W4 track.
- `MAX_BODY_BYTES` env-overridability — partially addressed by A1 (`HOOK_BLOCK_BUDGET`), `MAX_BODY_BYTES` remainder for W4.

### Test infrastructure currently in place

- `tests/smoke.sh` (1,690 LOC, existing, green)
- `tests/hook-latency.sh` (W1, in-process Go bench — regression check, not DoD surface)
- `tests/hook-latency-exec.sh` (W1, shell wall-clock — **DoD evidence surface**, p99=13.13ms)
- `tests/ack-collision.sh` (A2 regression gate — green post-fix)
- `tests/fail-open-fallback.sh` (B-1 regression gate, updated for goose-schema-version — green)
- `tests/utf8-budget-parity.sh` (idle-birch #3 regression gate — green post-bytes-fix)

Test-engineer's queue is empty unless round 2 surfaces new tests.

### CI workflows currently wired

- `.github/workflows/schema-portability.yml` — invokes `peer-inbox-migrate` binary against `postgres:16`; asserts `goose_db_version.version_id=1`, `meta.schema_version=10`, v2-reserve columns, re-apply idempotency.
- `.github/workflows/hook-latency.yml` — runs T1 + T2 (15ms budget) + T3 + T4 gates.

## Convergence history summary

**v3.0 plan (9 rounds + 2 challenge):**
- Language: Go (Elixir eliminated on hook cold-start; Rust wins below perceptibility threshold)
- Storage: SQLite today, Postgres-portable schema via goose (shipped), Postgres trajectory for cloud v2
- Protocol: protobuf 3 `.proto v1` at `docs/protocol/v1.proto`, exercised on Unix socket from day one
- Mode: hybrid — single-machine fast path preserved
- Owner framing: cloud-READY, not cloud-YET

**v3.1 roadmap (converged + 2 challenge passes):**
- v3.1: export + write-time redaction (with dual-path for pre-v3.1 rows) + stale-marker reaper + session-key disambiguation + peer-status-cheap-core
- v3.2: room-digest carryover + durable delivery
- v4: typed structured payloads → git-aware ReviewFinding → synthesis dashboards → conversation forking → capability-based routing

**v3.2 frontend (6-reviewer convergence, mediator-ratified gate):**
- v3.2-α killer: Synthesis Workbench (findings panel + position-lean history + stance-at-time stripe + turn-cap gauge + stale-session filter). 5-8d.
- v3.2-β: three-pane workbench frame + Convergence Replay. +2-3d.
- v3.2-γ: test-fixture inspector `?db=` with path safelist. 0.5-1d or 2-3h on top of workbench.
- v3.2-δ: stance-stripe seen-by extension. +1d, earn-its-way.
- **GATED on v3.1 redaction landing first.**

## How to resume (by role)

### If you're coder (or a fresh implementer taking over)

Coder's W1 work is complete as of the last session-complete broadcast. No in-flight code work. Check `git log` to see the goose rework + queued fixes landed.

Next steps when owner opens the next phase:
1. Read `plans/v3.0-go-cloud-ready-scoping.md` to see what's coming (W2: gRPC + Python bindings + wire-format parity matrix + capability handshake).
2. Read `plans/v3.1-roadmap-brainstorm.md` for post-v3.0 work (v3.1 redaction is next if owner wants it).
3. Watch mediator for the next work assignment. Do NOT start W2 until owner green-lights — the pause is explicit.
4. If round 2 reviewers surface findings you need to address, mediator will route them with severity + file:line + suggested fixes.

### If you're mediator (or a fresh mediator replacing me)

1. Re-register: `agent-collab session register --label mediator --agent claude --role mediator --pair-key dusty-island-8114`.
2. Read all three plan docs + this handoff.
3. Check `agent-collab peer list --include-stale` for active peers.
4. **Your current state: holding for owner's go-signal on round 2.** Coder has completed the queue; full test sweep is green; three decision points are pending (A2 session_key vs label, T3 fixture deviation, migrate binary size). Reviewers are standing down.
5. When owner gives go-signal for round 2: ping reviewers with the post-coder state + the three decision points as the scope under review. Synthesize findings. Ratify.
6. When owner gives a different go-signal (skip to W2, or defer, or close W1): execute that directive.
7. Remember the procedural hygiene rule: reviewer concurrence is input, not self-ratifying. Issue rulings explicitly under mediator authority.

### If you're a reviewer returning to the room

1. Re-register with your existing label + `--pair-key dusty-island-8114`.
2. Read the three plan docs + this handoff.
3. **Stand down until mediator pings you for round 2.** The pause is active.
4. When round 2 opens: review coder's session-complete broadcast + verify the 14 items + weigh in on the three decision points. File:line findings, severity, suggested fixes. No code edits.

### If you're the owner

The mediator is holding for your go-signal. All W1 work has shipped green. Next decisions on your plate:
- Open round 2 review? (lightweight — coder's work is clean, decision points above)
- Skip directly to v3.1 (redaction + export + papercuts)?
- Skip directly to W2 of v3.0 (gRPC + Python bindings + wire-format parity)?
- Pause the whole project?

The three coder-flagged decision points can also be answered by you directly without opening round 2:
1. A2 session_key vs label.
2. T3 fixture deviation (who owns the revert/ratify).
3. Migrate binary 19 MB vs `nopostgres` build-tag.

## Cleanup / hygiene when round 2 closes (or is skipped)

- Mediator doc edits queued:
  - Add "W1 phase closed 2026-04-18" marker to `plans/v3.0-go-cloud-ready-scoping.md`.
  - Add N1 to a "v2.proto hot list" section in `plans/v3.0-go-cloud-ready-scoping.md`.
  - Confirm all W1 DoD lines match shipped reality (already done in an earlier pass).
- Stale peer cleanup (alice, codex-side, joseph, urban-pine, gemini-dusty-v{2,3}) — v3.1 stale-marker auto-reaper will fix this; no manual action needed.

## References

- Peer-inbox room: `dusty-island-8114`
- Current `cmd_peer_web`: `scripts/peer-inbox-db.py:1829` (453 LOC, port 8787; v3.2 territory)
- Prior handoff docs: `plans/v1.7-integration-handoff.md`, `plans/v2.0-scoping.md`, `plans/v2.1-room-dynamics-handoff.md`
- Memory note (mediator-local): `~/.claude/projects/-Users-deeJ-Development/memory/project_agent_collab_v3_pause.md` — pause directive with decay check.
